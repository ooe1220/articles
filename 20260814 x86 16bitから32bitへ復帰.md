
# 目的
最近`SeaBIOS`と`GRUB`のソースコードを読んでいるのですが、`x86`CPUで32bitモードから16bitモードへ戻る処理が出てきて気になったので自分でも試してみました。

# 実験
16bitモードではBIOSの機能を`int`命令から呼び出して使用出来ますが、32bitモードへ切り替えると使用出来なくなってしまいます。
そこで今回は16bitモード→32bitモード→16bitモードと切り替えて、復帰後に`int 0x10`を用いて画面出力をし、無事出力されれば16bitモード復帰成功とします。
単純に復帰できるかの実験をする為、今回スタックの保存等は行いません。

# 参考

参考にしたのは`GRUB`の以下の部分です。
https://gitlab.freedesktop.org/gnu-grub/grub/-/tree/master

<details>
<summary>grub-core/kern/i386/realmode.S</summary>

```grub-core/kern/i386/realmode.S
prot_to_real:
	/* just in case, set GDT */
	lgdt	gdtdesc

	sidt    protidt
	lidt    LOCAL(realidt)

	/* save the protected mode stack */
	movl	%esp, %eax
	movl	%eax, protstack

	/* get the return address */
	movl	(%esp), %eax
	movl	%eax, GRUB_MEMORY_MACHINE_REAL_STACK

	/* set up new stack */
	movl	$GRUB_MEMORY_MACHINE_REAL_STACK, %eax
	movl	%eax, %esp
	movl	%eax, %ebp

	/* set up segment limits */
	movw	$GRUB_MEMORY_MACHINE_PSEUDO_REAL_DSEG, %ax
	movw	%ax, %ds
	movw	%ax, %es
	movw	%ax, %fs
	movw	%ax, %gs
	movw	%ax, %ss

	/* this might be an extra step */
	/* jump to a 16 bit segment */
	ljmp	$GRUB_MEMORY_MACHINE_PSEUDO_REAL_CSEG, $tmpcseg

tmpcseg:
	.code16

	/* clear the PE bit of CR0 */
	movl	%cr0, %eax
	andl 	$(~GRUB_MEMORY_CPU_CR0_PE_ON), %eax
	movl	%eax, %cr0

	/* flush prefetch queue, reload %cs */
	ljmpl	$0, $realcseg

realcseg:
	/* we are in real mode now
	 * set up the real mode segment registers : DS, SS, ES
	 */
	/* zero %eax */
	xorl	%eax, %eax

	movw	%ax, %ds
	movw	%ax, %es
	movw	%ax, %fs
	movw	%ax, %gs
	movw	%ax, %ss

#ifdef GRUB_MACHINE_PCBIOS
	/* restore interrupts */
	sti
#endif

	/* return on new stack! */
	retl
#endif
 *  GNU General Public License for more details.
 *
 *  You should have received a copy of the GNU General Public License
 *  along with GRUB.  If not, see <http://www.gnu.org/licenses/>.
 */

prot_to_real:
	/* just in case, set GDT */
	lgdt	gdtdesc

	sidt    protidt
	lidt    LOCAL(realidt)

	/* save the protected mode stack */
	movl	%esp, %eax
	movl	%eax, protstack

	/* get the return address */
	movl	(%esp), %eax
	movl	%eax, GRUB_MEMORY_MACHINE_REAL_STACK

	/* set up new stack */
	movl	$GRUB_MEMORY_MACHINE_REAL_STACK, %eax
	movl	%eax, %esp
	movl	%eax, %ebp

	/* set up segment limits */
	movw	$GRUB_MEMORY_MACHINE_PSEUDO_REAL_DSEG, %ax
	movw	%ax, %ds
	movw	%ax, %es
	movw	%ax, %fs
	movw	%ax, %gs
	movw	%ax, %ss

	/* this might be an extra step */
	/* jump to a 16 bit segment */
	ljmp	$GRUB_MEMORY_MACHINE_PSEUDO_REAL_CSEG, $tmpcseg

tmpcseg:
	.code16

	/* clear the PE bit of CR0 */
	movl	%cr0, %eax
	andl 	$(~GRUB_MEMORY_CPU_CR0_PE_ON), %eax
	movl	%eax, %cr0

	/* flush prefetch queue, reload %cs */
	ljmpl	$0, $realcseg

realcseg:
	/* we are in real mode now
	 * set up the real mode segment registers : DS, SS, ES
	 */
	/* zero %eax */
	xorl	%eax, %eax

	movw	%ax, %ds
	movw	%ax, %es
	movw	%ax, %fs
	movw	%ax, %gs
	movw	%ax, %ss

#ifdef GRUB_MACHINE_PCBIOS
	/* restore interrupts */
	sti
#endif

	/* return on new stack! */
	retl
#endif
	.code32

```

</details>


# 自分で書き直したコード


```test.asm
bits 16
org 0x7C00

start:

    cli

    xor ax, ax
    mov ss, ax
    mov sp, 0x7C00
    
    ; VGAテキストモード設定(mode3)
    mov ax, 0x0003
    int 0x10 
    
    ; 到達確認
    mov ah, 0x0E
    mov al, 'A'
    mov bh, 0
    mov bl, 0x07
    int 0x10

    ; GDT
    lgdt [gdt_descriptor]

    ; protected mode
    mov eax, cr0
    or eax, 1
    mov cr0, eax

    ; 16:32 far jump
    jmp 0x08:real_to_prot
    
; ==================================================
; Protected Mode
; ==================================================

bits 32

real_to_prot:

    mov ax, 0x10
    mov ds, ax
    mov es, ax
    mov fs, ax
    mov gs, ax
    mov ss, ax
    mov esp, 0x9FC00
    
    ; 32ビット到達確認(Cに上書きされる為2行目に表示)
    mov word [0xB8000 + 160], 0x0742    ; "B"
    
    ; 16ビットデータセグメントを設定する 0x20 = GDT[4]
    ; D=0なので命令を16ビットととして実行する
    mov ax, 0x20
    mov ds, ax
    mov es, ax
    mov fs, ax
    mov gs, ax
    mov ss, ax

    ; CSを16ビットコードセグメントに変更する 0x18 = GDT[3] 
    jmp 0x18:tmpcseg


; ==================================================
; Real Mode
; ==================================================

bits 16

tmpcseg:

    ; PE = 0
    mov eax, cr0
    and eax, 0xFFFFFFFE
    mov cr0, eax
    
    ; CS = 0として16bitモードへ突入
    jmp 0:real_mode

real_mode:

    xor ax, ax
    mov ds, ax
    mov es, ax
    mov ss, ax
    mov fs, ax
    mov gs, ax
    mov sp, 0x7C00
    
    sti

    ; 16ビット復帰確認
    mov ah, 0x0E
    mov al, 'C'
    mov bh, 0
    mov bl, 0x07
    int 0x10

stop:
    cli
    hlt
    jmp stop


; ==================================================
; GDT
; ==================================================

align 8

gdt_start:

    ; null
    dq 0

    ; --------------------------------------------------------
    ; GDT[1] - 32ビットコードセグメント
    ;
    ; セレクタ = 0x08
    ; ベース   = 0
    ; リミット = 0xFFFFF
    ; G       = 1
    ; D       = 1
    ; --------------------------------------------------------

    dw 0xFFFF
    dw 0x0000
    db 0x00
    db 0x9A
    db 0xCF
    db 0x00


    ; --------------------------------------------------------
    ; GDT[2] - 32ビットデータセグメント
    ;
    ; セレクタ = 0x10
    ; ベース   = 0
    ; リミット = 0xFFFFF
    ; --------------------------------------------------------

    dw 0xFFFF
    dw 0x0000
    db 0x00
    db 0x92
    db 0xCF
    db 0x00


    ; --------------------------------------------------------
    ; GDT[3] - 16ビットコードセグメント
    ;
    ; セレクタ = 0x18
    ; ベース   = 0
    ; リミット = 0xFFFF
    ; G       = 0
    ; D       = 0
    ; --------------------------------------------------------

    dw 0xFFFF
    dw 0x0000
    db 0x00
    db 0x9E
    db 0x00
    db 0x00


    ; --------------------------------------------------------
    ; GDT[4] - 16ビットデータセグメント
    ;
    ; セレクタ = 0x20
    ; ベース   = 0
    ; リミット = 0xFFFF
    ; --------------------------------------------------------

    dw 0xFFFF
    dw 0x0000
    db 0x00
    db 0x92
    db 0x00
    db 0x00


gdt_end:

gdt_descriptor:
    dw gdt_end - gdt_start - 1
    dd gdt_start


times 2048-($-$$) db 0 ;CDの為0xAA55は不要
```

# 実行結果

```
nasm -f bin test.asm -o test.bin

rmdir iso
rm os.iso

mkdir iso
cp test.bin iso/

xorriso -as mkisofs \
  -o os.iso \
  -b test.bin \
  -c boot.cat \
  -no-emul-boot \
  -boot-load-size 4 \
  iso
```

## QEMU

```
qemu-system-i386 -cdrom os.iso -m 512 -boot d
```
<img width="772" height="514" alt="截图 2026-08-14 20-57-57" src="https://github.com/user-attachments/assets/fcb65c55-4536-40bb-963b-7a5abe1e35bc" />


## dynabook SS M10 11L/2実機

実機では失敗しました。再起動を繰り返します。
QEMUでは条件が甘い可能性があるので今後実機で動かない原因を調べます。

```
sudo dvd+rw-format -blank=fast /dev/sr0
sudo growisofs -dvd-compat -Z /dev/sr0=os.iso
```
<img width="1706" height="1279" alt="_cgi-bin_mmwebwx-bin_webwxgetmsgimg__ MsgID=811821274875213024 skey=@crypt_e58c0397_616676524d5cdcec7066557d5da7256d mmweb_appid=wx_webfilehelper" src="https://github.com/user-attachments/assets/b5e9b833-cd1c-4a59-84a9-dcc559c27a9c" />




