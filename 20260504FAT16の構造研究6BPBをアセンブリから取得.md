# 目的

オフセットを指定してBPBから値を取得する方法を確かめる。

# BPBの形式

読みやすくする為に構造体で書く。

```c
// FAT16 BPB (BIOS Parameter Block)
// オフセットはブートセクタの先頭 (0x0000) からの相対位置

#pragma pack(push, 1)
typedef struct {
    // jmp命令とOEM識別子（BPBの前の部分）
    uint8_t  BS_jmpBoot[3];      // 0x00: 機械後へのジャンプ命令 (EB 3C 90)
    char     BS_OEMName[8];      // 0x03: フォーマットしたOS/ツールの識別子 (例: "MSDOS5.0")

    // BPB要素
    uint16_t BPB_BytsPerSec;     // 0x0B: 1セクタのバイト数 (通常512)
    uint8_t  BPB_SecPerClus;     // 0x0D: 1クラスタのセクタ数 (1,2,4,8,...,128)
    uint16_t BPB_RsvdSecCnt;     // 0x0E: 予約セクタ数 (FAT領域の前、通常1)
    uint8_t  BPB_NumFATs;        // 0x10: FATの個数 (通常2)
    uint16_t BPB_RootEntCnt;     // 0x11: ルートディレクトリのエントリ数 (FAT16では通常512)
    uint16_t BPB_TotSec16;       // 0x13: 総セクタ数 (32MB未満の場合。0なら後続フィールドを使用)
    uint8_t  BPB_Media;          // 0x15: メディア記述子 (0xF8=固定ディスク, 0xF0=リムーバブル)
    uint16_t BPB_FATSz16;        // 0x16: 1つのFATの大きさ（セクタ数）
    uint16_t BPB_SecPerTrk;      // 0x18: 1トラックあたりのセクタ数 (CHS用)
    uint16_t BPB_NumHeads;       // 0x1A: ヘッド数 (CHS用)
    uint32_t BPB_HiddSec;        // 0x1C: ボリューム前の隠蔽セクタ数
    uint32_t BPB_TotSec32;       // 0x20: 総セクタ数 (BPB_TotSec16が0の場合に使用)

    // FAT16 拡張BPB (EBPB)
    uint8_t  BS_DrvNum;          // 0x24: ドライブ番号 (INT 13h用)
    uint8_t  BS_Reserved1;       // 0x25: 予約領域
    uint8_t  BS_BootSig;         // 0x26: 拡張ブート署名 (0x29=次のフィールドが有効)
    uint32_t BS_VolID;           // 0x27: ボリュームシリアル番号
    char     BS_VolLab[11];      // 0x2B: ボリュームラベル
    char     BS_FilSysType[8];   // 0x36: ファイルシステム種類 (例: "FAT16   ")

    uint8_t  BootCode[448];      // 0x3E: 機械語
    uint16_t BS_BootSign;        // 0x1FE: 0xAA55

} FAT16_BPB;
#pragma pack(pop)
```

# アセンブリでBPB読み込み
```fat16_bpb.asm
section .data
    filename db "fat16.img", 0

section .bss
    buffer resb 512

section .text
    global _start

_start:

    ; open("fat16.img", O_RDONLY, 0)
    mov eax, 5          ; sys_open
    mov ebx, filename
    xor ecx, ecx        ; O_RDONLY
    xor edx, edx
    int 0x80
    mov esi, eax        ; fd保存

    ; read(fd, buffer, 512)
    mov eax, 3          ; sys_read
    mov ebx, esi
    mov ecx, buffer
    mov edx, 512
    int 0x80

    ; =========================
    ; BPB取り出し
    ; =========================

    mov ebx, buffer     ; 基底アドレス

    ; bytes per sector (offset 11, 2byte)
    movzx eax, word [ebx + 11]

    ; sectors per cluster (offset 13, 1byte)
    movzx ecx, byte [ebx + 13]

    ; reserved sectors (offset 14, 2byte)
    movzx edx, word [ebx + 14]

    ; number of FATs (offset 16, 1byte)
    movzx esi, byte [ebx + 16]

    ; root entries (offset 17, 2byte)
    movzx edi, word [ebx + 17]

    ; total sectors (offset 19, 2byte)
    movzx ebp, word [ebx + 19]

    ; =========================
    ; GDBで確認
    ; =========================
    int3

    ; exit(0)
    mov eax, 1
    xor ebx, ebx
    int 0x80
```

# 検証

```bash
nasm -f elf32 fat16_bpb.asm && ld -m elf_i386 -o fat16_bpb fat16_bpb.o
gdb ./fat16_bpb
run
info registers
```

```bash
linuxlite  ~  kaihatsu  2  SIGTSTP  gdb ./fat16_bpb
...(省略)...
(gdb) run
Starting program: /home/linuxlite/kaihatsu/fat16_bpb 

Program received signal SIGTRAP, Trace/breakpoint trap.
0x08049043 in _start ()
(gdb) info registers
eax            0x200               512
ecx            0x4                 4
edx            0x4                 4
ebx            0x804a00c           134520844
esp            0xffffd800          0xffffd800
ebp            0x5000              0x5000
esi            0x2                 2
edi            0x200               512
eip            0x8049043           0x8049043 <_start+67>
eflags         0x246               [ PF ZF IF ]
cs             0x23                35
ss             0x2b                43
ds             0x2b                43
es             0x2b                43
fs             0x0                 0
gs             0x0                 0
```

uint16_t BPB_BytsPerSec : `EAX` = `512` 1セクタ=512バイト...問題無し

uint8_t  BPB_SecPerClus : `ECX` = `4` 1クラスタ=4セクタ...問題無し

uint16_t BPB_RsvdSecCnt : `EDX` = `4`

uint8_t  BPB_NumFATs : `ESI` = `2` FAT表は2個...問題無し

uint16_t BPB_RootEntCnt : `EDI` = `512` ルートディレクトリ全登録数=512個...問題無し

uint16_t BPB_TotSec16 : `EBP` = `20480` 20480 × 512 = 10MB...問題無し

# 検証環境

機種 : VOSTRO 1540
CPU : Intel(R) Celeron(R) CPU        P4600  @ 2.00GHz
MEM : 1.8Gi
OS : Linux Lite 6.6
NASM : 2.15.05
GDB : GNU gdb (Ubuntu 12.1-0ubuntu1~22.04.2) 12.1
※実家に帰省中。昔のパソコン。
<img width="2358" height="3144" alt="d205a22585b38" src="https://github.com/user-attachments/assets/f7592fa0-1384-4709-ac7d-1302a49f380f" />


