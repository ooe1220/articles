# 初めに

筆者は現在任天堂GBA向けに`printf`関数を作ろうとしています。
今はまだ文字列入力機能しか作っていません。
[任天堂GBAで字体及び文字列を表示する仕組み](https://qiita.com/earthen94/items/3290dab6b65b0457d95a)

`printf`と名乗るからには引数の数を可変にしたいのですが、C言語で書いた引数がアセンブリ上でどう扱われているかを知る必要があります。

以下は前に筆者が32bitのx86向けに書いた`printf`です。32bitのx86はC言語の関数呼び出しで渡される引数が全てスタック上に積まれるので(コンパイラによってはレジスタを使うものもあるかもしれませんが)、ポインタでずらしながら引数を一つずつ取得することが可能でした。
しかしARMは速度改善の為か一部はレジスタ経由で渡されるらしいので、何個までがレジスタ経由、何個からがスタック経由となるのかを調べてみることにしました。
https://github.com/ooe1220/KansoOS/blob/master/src/x86/console.c
<details>
<summary>console.c</summary>

```console.c
void kprintf(const char* format, ...) {
    // 可変数引数
    char* arg_ptr = (char*)&format + sizeof(char*);
    const char* p = format;
    char buffer[32];
    
    while (*p) {
        if (*p == '%') {
            p++;
            switch (*p) {
                case '%':  // %% -> %
                    kputc('%');
                    break;
                    
                case 'c':  // %c -> 文字
                    kputc(*((char*)arg_ptr));
                    arg_ptr += sizeof(char);
                    break;
                    
                case 's':  // %s -> 文字列
                    kputs(*((char**)arg_ptr));
                    arg_ptr += sizeof(char*);
                    break;
                    
                case 'd':  // %d -> 符号付き整数
                case 'i':
                    itoa(*((int*)arg_ptr), buffer);
                    kputs(buffer);
                    arg_ptr += sizeof(int);
                    break;
                    
                case 'u':  // %u -> 符号無し整数
                    utoa(*((unsigned int*)arg_ptr), buffer, 10);
                    kputs(buffer);
                    arg_ptr += sizeof(unsigned int);
                    break;
                    
                case 'x':  // %x -> 16進数
                    utoa(*((unsigned int*)arg_ptr), buffer, 16);
                    kputs(buffer);
                    arg_ptr += sizeof(unsigned int);
                    break;
                    
                default:  // 未定義はそのまま出す
                    kputc('%');
                    kputc(*p);
                    break;
            }
        } else {
            kputc(*p);
        }
        p++;
    }
}
```

</details>


# 検証用C言語コード

```bash
arm-none-eabi-gcc -S main.c -o main.s -nostdlib -ffreestanding
```

```main.c
int func4(int a,int b, int c, int d){
    a=111;
    b=222;
    c=333;
    d=444;
    return 55;
}

int func6(int a,int b,int c,int d,int e,int f){
    a=111;
    b=222;
    c=333;
    d=444;
    e=555;
    f=666;
    return 77;
}

void main(){
    func4(11,22,33,44);
    func6(11,22,33,44,55,66);
}
```

# 生成されたアセンブリ

長いですが一旦全部貼ります。
<details>
<summary>main.s</summary>

```main.s
	.cpu arm7tdmi
	.eabi_attribute 20, 1
	.eabi_attribute 21, 1
	.eabi_attribute 23, 3
	.eabi_attribute 24, 1
	.eabi_attribute 25, 1
	.eabi_attribute 26, 1
	.eabi_attribute 30, 6
	.eabi_attribute 34, 0
	.eabi_attribute 18, 4
	.file	"main.c"
	.text
	.align	2
	.global	func4
	.arch armv4t
	.syntax unified
	.arm
	.fpu softvfp
	.type	func4, %function
func4:
	@ Function supports interworking.
	@ args = 0, pretend = 0, frame = 16
	@ frame_needed = 1, uses_anonymous_args = 0
	@ link register save eliminated.
	str	fp, [sp, #-4]!
	add	fp, sp, #0
	sub	sp, sp, #20
	str	r0, [fp, #-8]
	str	r1, [fp, #-12]
	str	r2, [fp, #-16]
	str	r3, [fp, #-20]
	mov	r3, #111
	str	r3, [fp, #-8]
	mov	r3, #222
	str	r3, [fp, #-12]
	ldr	r3, .L3
	str	r3, [fp, #-16]
	mov	r3, #444
	str	r3, [fp, #-20]
	mov	r3, #55
	mov	r0, r3
	add	sp, fp, #0
	@ sp needed
	ldr	fp, [sp], #4
	bx	lr
.L4:
	.align	2
.L3:
	.word	333
	.size	func4, .-func4
	.align	2
	.global	func6
	.syntax unified
	.arm
	.fpu softvfp
	.type	func6, %function
func6:
	@ Function supports interworking.
	@ args = 8, pretend = 0, frame = 16
	@ frame_needed = 1, uses_anonymous_args = 0
	@ link register save eliminated.
	str	fp, [sp, #-4]!
	add	fp, sp, #0
	sub	sp, sp, #20
	str	r0, [fp, #-8]
	str	r1, [fp, #-12]
	str	r2, [fp, #-16]
	str	r3, [fp, #-20]
	mov	r3, #111
	str	r3, [fp, #-8]
	mov	r3, #222
	str	r3, [fp, #-12]
	ldr	r3, .L7
	str	r3, [fp, #-16]
	mov	r3, #444
	str	r3, [fp, #-20]
	ldr	r3, .L7+4
	str	r3, [fp, #4]
	ldr	r3, .L7+8
	str	r3, [fp, #8]
	mov	r3, #77
	mov	r0, r3
	add	sp, fp, #0
	@ sp needed
	ldr	fp, [sp], #4
	bx	lr
.L8:
	.align	2
.L7:
	.word	333
	.word	555
	.word	666
	.size	func6, .-func6
	.align	2
	.global	main
	.syntax unified
	.arm
	.fpu softvfp
	.type	main, %function
main:
	@ Function supports interworking.
	@ args = 0, pretend = 0, frame = 0
	@ frame_needed = 1, uses_anonymous_args = 0
	push	{fp, lr}
	add	fp, sp, #4
	sub	sp, sp, #8
	mov	r3, #44
	mov	r2, #33
	mov	r1, #22
	mov	r0, #11
	bl	func4
	mov	r3, #66
	str	r3, [sp, #4]
	mov	r3, #55
	str	r3, [sp]
	mov	r3, #44
	mov	r2, #33
	mov	r1, #22
	mov	r0, #11
	bl	func6
	nop
	sub	sp, fp, #4
	@ sp needed
	pop	{fp, lr}
	bx	lr
	.size	main, .-main
	.ident	"GCC: (15:10.3-2021.07-4) 10.3.1 20210621 (release)"
```

</details>

※変数の代入に`mov`を使う場合と`ldr`を使う場合がある理由は、前に以下の記事で検証しています。
[32ビットARMアセンブリのMOV命令で使える即値の仕組みを理解する](https://qiita.com/earthen94/items/abb68a6bc83819084696)

## 引数4個

変数の取り出しと代入:
```s
	str	r0, [fp, #-8]
	str	r1, [fp, #-12]
	str	r2, [fp, #-16]
	str	r3, [fp, #-20]
	mov	r3, #111
	str	r3, [fp, #-8]
	mov	r3, #222
	str	r3, [fp, #-12]
	ldr	r3, .L7
	str	r3, [fp, #-16]
	mov	r3, #444
	str	r3, [fp, #-20]

; ...略...

.L3:
	.word	333
```

返り値:
```
	mov	r3, #55
	mov	r0, r3
```

呼び出し側:
```s
;func4(11,22,33,44);
	mov	r3, #44
	mov	r2, #33
	mov	r1, #22
	mov	r0, #11
	bl	func4
```

## 引数6個

変数の取り出しと代入:
```s
	str	r0, [fp, #-8]
	str	r1, [fp, #-12]
	str	r2, [fp, #-16]
	str	r3, [fp, #-20]
	mov	r3, #111
	str	r3, [fp, #-8]
	mov	r3, #222
	str	r3, [fp, #-12]
	ldr	r3, .L7
	str	r3, [fp, #-16]
	mov	r3, #444
	str	r3, [fp, #-20]
	ldr	r3, .L7+4
	str	r3, [fp, #4]
	ldr	r3, .L7+8
	str	r3, [fp, #8]
	
; ...略...

.L7:
	.word	333
	.word	555
	.word	666
```

呼び出し側:
```s
	mov	r3, #66
	str	r3, [sp, #4]
	mov	r3, #55
	str	r3, [sp]
	mov	r3, #44
	mov	r2, #33
	mov	r1, #22
	mov	r0, #11
	bl	func6
```

# 結果
引数1〜4は`r0`〜`r3`、
5個目からスタック経由となる事が分かりました。
