[8051エミュレータEdSim51DL導入手順](https://qiita.com/earthen94/items/bb2e134a2b0130f7732a)

```asm
ORG 0000H
        SJMP START

START:
        MOV DPTR,#SEGTAB   ; DPTRにled字体表の先頭アドレス格納
        MOV R0,#0          ; 表示する文字の索引

LOOP:
        MOV A,R0
        MOVC A,@A+DPTR     ; 字体をAに入れる @A=A+DPTRで計算した番地の中身
        MOV P1,A           ; 7セグ表示
        ACALL DELAY

        INC R0             ; 索引R0を1進める
        CJNE R0,#15,LOOP   ; R0 ≠ 15 なら LOOP
        MOV R0,#0          ; R0=15(F)なら索引を初期化
        SJMP LOOP

; 1～F led字体表
SEGTAB:
        DB 0F9H  ; 1
        DB 0A4H  ; 2
        DB 0B0H  ; 3
        DB 99H   ; 4
        DB 92H   ; 5
        DB 82H   ; 6
        DB 0F8H  ; 7
        DB 80H   ; 8
        DB 90H   ; 9
        DB 88H   ; A
        DB 83H   ; b
        DB 0C6H  ; C
        DB 0A1H  ; d
        DB 86H   ; E
        DB 8EH   ; F
        
DELAY:
    MOV R7,#1
D1:
    DJNZ R7,D1
    RET
```

<img width="1553" height="1089" alt="image" src="https://github.com/user-attachments/assets/b36341a6-70c2-4df6-99a0-472e29603934" />

