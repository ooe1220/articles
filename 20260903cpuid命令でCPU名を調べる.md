
# 目的

`SeaBIOS`のソースを読んでいるとこんな処理を見つけました。
```src/x86.c
#include "x86.h" // __cpuid

void
cpuid(u32 index, u32 *eax, u32 *ebx, u32 *ecx, u32 *edx)
{
    // Check for cpu id
    u32 origflags = save_flags();
    restore_flags(origflags ^ F_ID);
    u32 newflags = save_flags();
    restore_flags(origflags);

    if (((origflags ^ newflags) & F_ID) != F_ID)
        // no cpuid
        *eax = *ebx = *ecx = *edx = 0;
    else
        __cpuid(index, eax, ebx, ecx, edx);
}
```

`x86`CPUで`cpuid`命令を使用するとCPUの種類をASCIIコードで取得できるらしいので試して見ます。
Ubuntu及びベアメタルの両方で試すことにしました。

# ベアメタル

```bash
nasm -f bin cpuid_name.asm -o boot.bin

rm -r iso
mkdir iso
cp boot.bin iso/

xorriso -as mkisofs \
  -o os.iso \
  -b boot.bin \
  -c boot.cat \
  -no-emul-boot \
  -boot-load-size 4 \
  iso
```

```cpuid_name.asm
[org 0x7C00]
bits 16

start:
    cli
    xor ax, ax
    mov ds, ax
    mov ss, ax
    mov sp, 0x7C00
    mov ax, 0xB800 ; VRAM
    mov es, ax
    xor di, di     ; 文字列の表示位置
    sti
    
    xor ax, ax
    mov al, 0x03
    int 0x10
    
    ; --- 1回目: 0x80000002 ---
    mov eax, 0x80000002
    cpuid
    mov [cpu_brand +  0], eax
    mov [cpu_brand +  4], ebx
    mov [cpu_brand +  8], ecx
    mov [cpu_brand + 12], edx

    ; --- 2回目: 0x80000003 ---
    mov eax, 0x80000003
    cpuid
    mov [cpu_brand + 16], eax
    mov [cpu_brand + 20], ebx
    mov [cpu_brand + 24], ecx
    mov [cpu_brand + 28], edx

    ; --- 3回目: 0x80000004 ---
    mov eax, 0x80000004
    cpuid
    mov [cpu_brand + 32], eax
    mov [cpu_brand + 36], ebx
    mov [cpu_brand + 40], ecx
    mov [cpu_brand + 44], edx

    ; --- 画面に表示 ---
    mov si, cpu_brand
    call print_string

.hang:
    cli
    hlt
    jmp .hang


; =========================================
; print_string
; SI = 文字列
; =========================================
print_string:
    push ax
.loop:
    lodsb
    cmp al, 0
    je .done
    call putc
    jmp .loop
.done:
    pop ax
    ret


; =========================================
; putc
; AL = 文字
; =========================================
putc:
    mov ah, 0x0C
    mov [es:di], ax
    add di, 2
    ret

cpu_brand   times 48 db 0

times 2048-($-$$) db 0
```

## QEMU

```bash
qemu-system-i386 -cdrom os.iso -m 512 -boot d
```
<img width="810" height="548" alt="截图 2026-09-03 22-49-16" src="https://github.com/user-attachments/assets/00de4642-98d3-4e1f-8e21-2ad80804c876" />


## dynabook SS M10 11L/2

```bash
sudo dvd+rw-format -blank=fast /dev/sr0
sudo growisofs -dvd-compat -Z /dev/sr0=os.iso
```

<img width="2048" height="1536" alt="0c337668abd858" src="https://github.com/user-attachments/assets/741e14af-1276-4358-b364-6844d6a89175" />


# Ubuntu

```bash
as -o cpuid_name.o cpuid_name.s
ld -o cpuid_name cpuid_name.o
./cpuid_name
```

<img width="828" height="291" alt="截图 2026-09-03 22-48-32" src="https://github.com/user-attachments/assets/cc945e20-0a92-4379-b31e-05bfec9ba98b" />


```cpuid_name.s
.intel_syntax noprefix

.section .bss
    .lcomm cpu_brand, 48   # 48バイトのバッファを確保

.section .text
.global _start

_start:
    # --- CPUIDでCPU名を取得 1文字=1byte 32bit=4byte=4文字 ---
    mov eax, 0x80000002
    cpuid
    mov [rip + cpu_brand], eax
    mov [rip + cpu_brand + 4], ebx
    mov [rip + cpu_brand + 8], ecx
    mov [rip + cpu_brand + 12], edx

    mov eax, 0x80000003
    cpuid
    mov [rip + cpu_brand + 16], eax
    mov [rip + cpu_brand + 20], ebx
    mov [rip + cpu_brand + 24], ecx
    mov [rip + cpu_brand + 28], edx

    mov eax, 0x80000004
    cpuid
    mov [rip + cpu_brand + 32], eax
    mov [rip + cpu_brand + 36], ebx
    mov [rip + cpu_brand + 40], ecx
    mov [rip + cpu_brand + 44], edx

    # --- CPU名を出力 ---
    mov rax, 1             # sys_write
    mov rdi, 1             # stdout
    lea rsi, [rip + cpu_brand]  # バッファのアドレス
    mov rdx, 48            # 長さ（最大48バイト）
    syscall

    # --- 改行を出力 ---
    mov rax, 1
    mov rdi, 1
    lea rsi, [rip + newline]
    mov rdx, 1
    syscall

    # --- 終了 ---
    mov rax, 60            # sys_exit
    xor edi, edi           # ステータス 0
    syscall

.section .data
    newline: .ascii "\n"
```




