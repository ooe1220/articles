
# 初めに

前に以下の記事でブートローダはBIOSによってメモリ上の`0x07c00`へ読み込まれることを説明しました。
BIOSを改造して別の位置に読み込まれる実験もしましたが、基本的には`0x07c00`へ読み込まれます。
[ブートローダのORG0x7C00とは何か？BIOSを改造して実験](https://qiita.com/earthen94/items/0f517a0f6bebdbdea8f8)

BIOSによって`CS=0x0000,IP=0x7c00`になる場合と`CS=0x07c0,IP=0x0000`になる場合があります。
以下の記事でパソコンによって2通りあることを証明しています。
[DVD起動のブートセクタが何処に読み込まれるか検証](https://qiita.com/earthen94/items/a4a2cdfe769cfce2bb80)

最近GRUBのソースコードを読んでいます。
https://gitlab.freedesktop.org/gnu-grub/grub/-/tree/master

```
git clone https://gitlab.freedesktop.org/gnu-grub/grub.git
```


このようなコードを発見しました。
英語は読めないので機械翻訳しました。
> 次の命令へ ljmp する。なぜなら、一部のおかしな（不適切な）BIOSが、0000:7C00 ではなく 07C0:0000 へジャンプしてくるからである。

```grub-core/boot/i386/pc/boot.S
...(省略)...
1:
	/*
	 * ljmp to the next instruction because some bogus BIOSes
	 * jump to 07C0:0000 instead of 0000:7C00.
	 */
	ljmp	$0, $real_start

real_start:

	/* set up %ds and %ss as offset from 0 */
	xorw	%ax, %ax
	movw	%ax, %ds
	movw	%ax, %ss
...(省略)...
```

# 実験
 今回はこの部分だけを動かして検証してみます。
筆者が所有しているBIOSはちょうど`CS=0x07c0,IP=0x0000`へ読み込む仕様なので、実機で試してみます。

<img width="1706" height="1279" alt="微信图片_20260820201131_115_12" src="https://github.com/user-attachments/assets/976f8376-0202-4fa8-aef4-bfda5174bf8f" />

```bash
nasm -f bin boot.asm -o boot.bin

mkdir iso
cp boot.bin iso/

xorriso -as mkisofs \
  -o os.iso \
  -b boot.bin \
  -c boot.cat \
  -no-emul-boot \
  -boot-load-size 4 \
  iso

sudo dvd+rw-format -blank=fast /dev/sr0
sudo growisofs -dvd-compat -Z /dev/sr0=os.iso
```
 
```boot.asm
[org 0x7C00]
bits 16

start:
    cli

    ; STACK
    xor ax, ax
    mov ss, ax
    mov sp, 0x9000

    push cs
    pop ds

    ; VRAM(ES : DI)
    mov ax, 0xB800
    mov es, ax
    xor di, di

    ; CS表示
    mov ax, cs
    call print_hex16

    mov al, ':'
    call putc

    ; IP表示
    call here          ; CALL=PUSH IP + JMP
here:
    pop ax              ; AX = 現在IP

    call print_hex16
    
    
    jmp 0x0000:real_start
real_start:

    ; CS表示
    mov ax, cs
    call print_hex16

    mov al, ':'
    call putc

    ; IP表示
    call here2          ; CALL=PUSH IP + JMP
here2:
    pop ax              ; AX = 現在IP

    call print_hex16

.hang:
    cli
    hlt
    jmp .hang

; =========================================
; putc
; AL = 文字
; =========================================
putc:
    mov ah, 0x0C ; 赤 
    mov [es:di], ax
    add di, 2
    ret


; =========================================
; print_hex16
; AXを16進4桁で表示
; =========================================
print_hex16:
    push ax
    push bx
    push cx
    push dx

    mov bx, ax
    mov cx, 4 ; 2バイトの16進数表記は4文字

.loop:
    rol bx, 4

    mov dl, bl
    and dl, 0x0F

    cmp dl, 9
    jbe .num

    add dl, 'A' - 10
    jmp .out

.num:
    add dl, '0'

.out:
    mov al, dl
    call putc

    loop .loop
    
    mov al, ' '
    call putc

    pop dx
    pop cx
    pop bx
    pop ax
    ret

times 510-($-$$) db 0
dw 0xAA55
```
