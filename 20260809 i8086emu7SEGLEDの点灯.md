i8086emu
`https://sourceforge.net/projects/i8086emu/`から入手


`7SEGLED`のポート番号が分からない為、総当たりで調べる。
`CPU`速度を落として`DX`の値を凝視して`LED`が光った瞬間の`DX`を記録する

```led.asm
; nasm -f bin led.asm -o led.com
org 0h

mov dx, 0

next:
    mov al, 0FFh
    out dx, al

    mov cx, 30000
delay:
    loop delay

    inc dx
    cmp dx, 100h
    jb next

    jmp $
```

1つ目は`91h`,2つ目は`93h`と判明した。
```led.asm
org 0h

    mov al, 0FFh
    out 091h, al
    
    mov al, 0FFh
    out 093h, al
```

<img width="851" height="693" alt="image" src="https://github.com/user-attachments/assets/6d0467e5-64ae-47a7-bb52-9f8f15564be2" />

