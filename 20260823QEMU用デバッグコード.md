
# QEMU レジスタ値確認

`-monitor stdio`オプションを付ける

```asm
    ; ここで止めてinfo registers
     jmp $
```

# 到達確認 

## 16bit asm

```asm
    mov ax, 0xB800
    mov es, ax
    xor di, di          ; ES:DI = 0xB800:0x0000
    mov byte [es:di], 'E'
    mov byte [es:di+1], 0x07  ; 属性：白文字／黒背景
totatsu:
    cli
    hlt
    jmp totatsu
```

## 32bit asm

```asm
    mov byte [0xB8000], 'D'
    mov byte [0xB8000+1], 0x07  ; 属性：白文字／黒背景
totatsu:
    cli
    hlt
    loop totatsu
```

## 32bit C

```c
  volatile unsigned char *vga = (volatile unsigned char *)0xB8000;
  for (int i = 0; i < 6; i++) {
      vga[i * 2] = 'A';
      vga[i * 2 + 1] = 0x07;
  }
  for(;;);
```

