GCC の -m16 は本物の 8086 向けではない ― BCC と radare2 で確かめる 16bit 機械語

# 目的

SeaBIOSのソースを読み、改造しようとしています。
[SeaBIOSをソースからビルドしQEMUで動作](https://qiita.com/earthen94/items/57800600a2b77523475d)

SeaBIOSの中で16ビットと32ビットのコードを頻繁に切り替えているのですが、その部分を理解するため、今回は16ビット向けに生成した機械語がどうなるのかを調べます。

# GCCでの16ビット及び32ビットの比較

## Cソース及びコマンド

```
# 16bit 向け
gcc -O0 -m16 -march=i386 -ffreestanding -c test.c -o test16.o

# 1. リンクして実行可能ファイルにする
ld -m elf_i386 -Ttext 0x0 test16.o -o test16.elf

# 2. そこから生バイナリを抜く
objcopy -O binary test16.elf test16.bin

# 3. ndisasm で読む > testdis16.txt
ndisasm -b 16 test16.bin

# 32bit フラット向け
gcc -O0 -m32 -march=i386 -ffreestanding -c test.c -o test32.o

# 逆アセンブル
objdump -d -M intel test32.o
```

objcopy -O binary test16.o test16.bin
ndisasm -b 16 test16.bin

```test.c
int add(int a, int b)
{
    int x = a;
    x = x + b;
    return x;
}

```

## 16ビット

```
00000000  6655              push ebp
00000002  6689E5            mov ebp,esp
00000005  6683EC10          sub esp,byte +0x10
00000009  66E823000000      call dword 0x32
0000000F  6605F12F0000      add eax,0x2ff1
00000015  67668B4508        mov eax,[ebp+0x8]
0000001A  67668945FC        mov [ebp-0x4],eax
0000001F  67668B450C        mov eax,[ebp+0xc]
00000024  67660145FC        add [ebp-0x4],eax
00000029  67668B45FC        mov eax,[ebp-0x4]
0000002E  66C9              o32 leave
```

## 32ビット

```
Disassembly of section .text:

00000000 <add>:
   0:	55                   	push   ebp
   1:	89 e5                	mov    ebp,esp
   3:	83 ec 10             	sub    esp,0x10
   6:	e8 fc ff ff ff       	call   7 <add+0x7>
   b:	05 01 00 00 00       	add    eax,0x1
  10:	8b 45 08             	mov    eax,DWORD PTR [ebp+0x8]
  13:	89 45 fc             	mov    DWORD PTR [ebp-0x4],eax
  16:	8b 45 0c             	mov    eax,DWORD PTR [ebp+0xc]
  19:	01 45 fc             	add    DWORD PTR [ebp-0x4],eax
  1c:	8b 45 fc             	mov    eax,DWORD PTR [ebp-0x4]
  1f:	c9                   	leave  
  20:	c3                   	ret    

Disassembly of section .text.__x86.get_pc_thunk.ax:

00000000 <__x86.get_pc_thunk.ax>:
   0:	8b 04 24             	mov    eax,DWORD PTR [esp]
   3:	c3                   	ret    
```

## 結果
16ビットモードで32ビット命令を動かす時は先頭に`66`や`6766`がついた。


# 実際の8086ではどんな機械語となるかを確かめる

上でコンパイルした機械語は`386`以降の`16bit`モード用であり、8086向けではありません。
そもそも8086にEAX等の32ビットレジスタは存在しません。
次は実際の本物の16ビットである`8086(V30互換CPU)`で同じC言語をコンパイル及び逆アセンブルしてどのような機械語となるかを確かめます。



## BCCから生成した機械語が8086向けであることの証明

V30(8086互換)CPU上で動作したことから、BCCが生成した機械語が8086向けであると分かる。
<img width="1652" height="1014" alt="截图 2026-07-25 13-54-18" src="https://github.com/user-attachments/assets/88441c94-bd50-408d-8b53-874a188a1768" />

<img width="4096" height="3072" alt="bf0a7512cfb4f" src="https://github.com/user-attachments/assets/6ab7a253-3675-44a4-a734-0ba0475eb572" />

```test.c
#include<stdio.h>

int add(int a,int b){
    int x=a;
    x=x+b;
    return x;
}

int main(){
    int res=add(2,3);
    printf("res=%d",res);
    return 0;
}
```

## printfを消してADD関数の機械語を探す

printfがあると色々な関数を静的リンクして目的の機械語を探しづらくなるので消す。

r2コマンドを使って地道に探す(かなり大変だった)
機械語の先頭に`66`等がついていない。
これで8086上で動く命令と、gccが生成するi386以上の16ビット向けの機械語は別物であることが分かった。

```
[0000:001a]> s fcn.00000293; pdf
            ; CALL XREF from fcn.000002a4 @ 0x2ac(x)
┌ 17: fcn.00000293 (int16_t arg_4h, int16_t arg_6h);
│ `- args(sp[0x2..0x4])
│           0000:0293      55             push bp
│           0000:0294      8bec           mov bp, sp
│           0000:0296      8b5604         mov dx, word [arg_4h]
│           0000:0299      8bc2           mov ax, dx
│           0000:029b      034606         add ax, word [arg_6h]
│           0000:029e      8bd0           mov dx, ax
│           0000:02a0      8bc2           mov ax, dx
│           0000:02a2      5d             pop bp
└           0000:02a3      c3             ret
```


# 参考にした過去の記事

MS-DOS上でコンパイルをするのは非常に面倒な為、XP上で行います。
[UBUNTU上でQEMUを用いてXPをインストール](https://qiita.com/earthen94/items/303bf9a56244ee267497)
[x86 16bit C言語での開発環境](https://qiita.com/earthen94/items/b5d1db76274ab3aafa92)

# radare2の導入

Ubuntu上で`TEST.EXE`を逆アセンブルする為に使用します。
```
git clone https://gitcode.com/gh_mirrors/ra/radare2
cd radare2
sudo ./sys/install.sh
r2 -v
```

# XPの起動(起動ディスクの作り方は過去記事を参照)

仮想FDD(Aドライブ)経由でファイルやりとり
```
  qemu-system-i386 \
  -m 1024 \
  -hda xpjp.img \
  -boot c \
  -fda floppy.img \
  -cpu host \
  -smp 2 \
  -net nic -net user \
  -vga std \
  -rtc base=localtime \
  -enable-kvm \
  -usbdevice tablet \
  -cdrom transfer.iso
```





