
# 目的

最近`8086`向けの`CP/M86`というOSがあったことを知りました。
以下のサイトからFDDイメージを取得できます。
https://archive.org/details/7a4d81cd290caca537b4853787cc2024719e92c824ff0314c782461ae39213ee_202107

筆者は`8086`の互換CPUである`V30`が搭載されたパソコンを所有しています。
元々`MS-DOS`を動作させる為に、当時のチップを集めて作られた復刻機で`FDD`ドライブが無く、`HDD`扱いの`CFカード`からしか起動できません。
ディスク読み込みさえ解決出来れば理論上は動くはずなので、第一段階としてブートローダを取り出し解析してみました。

# 機械語

以下はブートローダを見る為に使用したLinuxコマンドです。
```bash
# ブートセクタ(先頭512バイト)を取り出す
dd if=CPM86_10.IMG of=head512.bin bs=512 count=1

# 逆アセンブル
objdump -D -b binary -m i8086 -M intel head512.bin

# バイナリで見る
hexdump -C head512.bin
```


見た感じ`0`〜`94`が実行コードの様です。
`95`以下は無理やりバリナリを命令として解釈しているだけで命令ではありません。

```bash
test@test-fujitsu:~/kaihatsu$ objdump -D -b binary -m i8086 -M intel head512.bin
 
head512.bin：     文件格式 binary


Disassembly of section .data:

00000000 <.data>:
   0:	fa                   	cli    
   1:	fc                   	cld    
   2:	8c c8                	mov    ax,cs
   4:	8e d8                	mov    ds,ax
   6:	ba 00 00             	mov    dx,0x0
   9:	8e d2                	mov    ss,dx
   b:	bc 00 7c             	mov    sp,0x7c00
   e:	fb                   	sti    
   f:	be 9a 7c             	mov    si,0x7c9a
  12:	e8 6f 00             	call   0x84
  15:	b8 51 00             	mov    ax,0x51
  18:	8e c0                	mov    es,ax
  1a:	b8 00 00             	mov    ax,0x0
  1d:	cd 13                	int    0x13
  1f:	72 5b                	jb     0x7c
  21:	b0 07                	mov    al,0x7
  23:	bb 00 00             	mov    bx,0x0
  26:	b9 02 00             	mov    cx,0x2
  29:	e8 35 00             	call   0x61
  2c:	81 c3 00 0e          	add    bx,0xe00
  30:	b0 04                	mov    al,0x4
  32:	b9 05 01             	mov    cx,0x105
  35:	e8 29 00             	call   0x61
  38:	81 c3 00 08          	add    bx,0x800
  3c:	b0 08                	mov    al,0x8
  3e:	b9 01 02             	mov    cx,0x201
  41:	e8 1d 00             	call   0x61
  44:	81 c3 00 10          	add    bx,0x1000
  48:	b0 08                	mov    al,0x8
  4a:	b9 01 03             	mov    cx,0x301
  4d:	e8 11 00             	call   0x61
  50:	81 c3 00 10          	add    bx,0x1000
  54:	b0 08                	mov    al,0x8
  56:	b9 01 04             	mov    cx,0x401
  59:	e8 05 00             	call   0x61
  5c:	2e ff 2e 95 7c       	jmp    DWORD PTR cs:0x7c95
  61:	50                   	push   ax
  62:	53                   	push   bx
  63:	8a c5                	mov    al,ch
  65:	04 30                	add    al,0x30
  67:	b4 0e                	mov    ah,0xe
  69:	bb 00 00             	mov    bx,0x0
  6c:	cd 10                	int    0x10
  6e:	b8 20 0e             	mov    ax,0xe20
  71:	cd 10                	int    0x10
  73:	5b                   	pop    bx
  74:	58                   	pop    ax
  75:	b4 02                	mov    ah,0x2
  77:	cd 13                	int    0x13
  79:	72 01                	jb     0x7c
  7b:	c3                   	ret    
  7c:	be c9 7c             	mov    si,0x7cc9
  7f:	e8 02 00             	call   0x84
  82:	eb 91                	jmp    0x15
  84:	53                   	push   bx
  85:	ac                   	lods   al,BYTE PTR ds:[si]
  86:	0a c0                	or     al,al
  88:	74 09                	je     0x93
  8a:	b4 0e                	mov    ah,0xe
  8c:	bb 00 00             	mov    bx,0x0
  8f:	cd 10                	int    0x10
  91:	eb f2                	jmp    0x85
  93:	5b                   	pop    bx
  94:	c3                   	ret    
  95:	00 25                	add    BYTE PTR [di],ah
  97:	51                   	push   cx
  98:	00 00                	add    BYTE PTR [bx+si],al
  9a:	0d 0a 43             	or     ax,0x430a


...(省略)...
```

# 文字列

バイナリを確認すると下の方に画面上に表示する文字列が格納されているのが見えます。
```bash
test@test-fujitsu:~/kaihatsu$ hexdump -C head512.bin
00000000  fa fc 8c c8 8e d8 ba 00  00 8e d2 bc 00 7c fb be  |.............|..|
00000010  9a 7c e8 6f 00 b8 51 00  8e c0 b8 00 00 cd 13 72  |.|.o..Q........r|
00000020  5b b0 07 bb 00 00 b9 02  00 e8 35 00 81 c3 00 0e  |[.........5.....|
00000030  b0 04 b9 05 01 e8 29 00  81 c3 00 08 b0 08 b9 01  |......).........|
00000040  02 e8 1d 00 81 c3 00 10  b0 08 b9 01 03 e8 11 00  |................|
00000050  81 c3 00 10 b0 08 b9 01  04 e8 05 00 2e ff 2e 95  |................|
00000060  7c 50 53 8a c5 04 30 b4  0e bb 00 00 cd 10 b8 20  ||PS...0........ |
00000070  0e cd 10 5b 58 b4 02 cd  13 72 01 c3 be c9 7c e8  |...[X....r....|.|
00000080  02 00 eb 91 53 ac 0a c0  74 09 b4 0e bb 00 00 cd  |....S...t.......|
00000090  10 eb f2 5b c3 00 25 51  00 00 0d 0a 43 50 2f 4d  |...[..%Q....CP/M|
000000a0  2d 38 36 20 42 6f 6f 74  73 74 72 61 70 20 4c 6f  |-86 Bootstrap Lo|
000000b0  61 64 65 72 20 31 2e 30  0d 0a 52 65 61 64 69 6e  |ader 1.0..Readin|
000000c0  67 20 54 72 61 63 6b 20  00 0d 0a 07 44 69 73 6b  |g Track ....Disk|
000000d0  20 45 72 72 6f 72 20 64  75 72 69 6e 67 20 42 6f  | Error during Bo|
000000e0  6f 74 73 74 72 61 70 2e  2e 2e 20 52 65 74 72 79  |otstrap... Retry|
000000f0  69 6e 67 2e 0d 0a 00 00  00 00 00 00 00 00 00 00  |ing.............|
00000100  e5 e5 e5 e5 e5 e5 e5 e5  e5 e5 e5 e5 e5 e5 e5 e5  |................|
*
000001f0  e5 e5 e5 e5 e5 e5 e5 e5  e5 e5 e5 e5 e5 e5 e5 00  |................|
00000200
test@test-fujitsu:~/kaihatsu$ 
```

# 8086上で実行してみる。

筆者の使用している`8086(V30)`機は`XTIDE BIOS`が入っており、`510`、`511`バイト目に`aa55`が無いとブートローダとして認識しないため書き込みます。

```bash
printf '\x55\xAA' | dd of=CPM86_10.IMG bs=1 count=2 seek=510 conv=notrunc
```

ブートローダの最後に書き込まれた。
```
000001f0  e5 e5 e5 e5 e5 e5 e5 e5  e5 e5 e5 e5 e5 e5 55 aa  |..............U.|
```

CFカードに書き込んで起動
```bash
lsblk
sudo dd if=CPM86_10.IMG of=/dev/sdb bs=512 conv=notrunc
```

ディスクの読み込みエラーは出ますが、少なくともブートローダの機械語は正しく実行されています。
エラーの原因は恐らくCFカード(HDD扱い)から起動しているのに、`int0x13`呼び出し部分がフロッピー向けの設定をしているからだと思われます。

<img width="4096" height="3072" alt="65ec82386fdef" src="https://github.com/user-attachments/assets/3f503daa-7d9f-4517-bbe9-7256bef5b2e5" />

# QEMU上にてフロッピーとして起動

```
qemu-system-i386 -fda CPM86_10.IMG -boot a
```

問題無く起動した為、問題はディスク読み込みのみだと思われます。
<img width="772" height="514" alt="截图 2026-07-25 20-39-16" src="https://github.com/user-attachments/assets/25fbcda2-adc1-4133-a58d-5cb50fc986ab" />


# 機械語の内容を解析

## 初期化

```
   0:	fa                   	cli              ; 割り込み禁止
   1:	fc                   	cld              ; 方向フラグ=0
   2:	8c c8                	mov    ax,cs     ; DS=CS
   4:	8e d8                	mov    ds,ax
   6:	ba 00 00             	mov    dx,0x0    ; SS:SP=0x0000:0x7C00
   9:	8e d2                	mov    ss,dx
   b:	bc 00 7c             	mov    sp,0x7c00
   e:	fb                   	sti              ; 割り込み許可
```

## 文字列表示関数の呼び出し
   
```
   f:	be 9a 7c             	mov    si,0x7c9a ; SI=CP/M-86 Bootstrap Loader 1.0\r\n"の1文字目
  12:	e8 6f 00             	call   0x84
```

## ディスク初期化

```
  15:	b8 51 00             	mov    ax,0x51
  18:	8e c0                	mov    es,ax
  1a:	b8 00 00             	mov    ax,0x0
  1d:	cd 13                	int    0x13
  1f:	72 5b                	jb               ; キャリーフラグが立っていたら（＝失敗）、0x7c のエラー処理へ跳ぶ
```

## カーネルの読み込み

CP/M-86カーネルをディスク上の5箇所から読み出し、メモリ上の連続領域に綺麗に並べる

1回目：トラック0・セクタ2から7セクタ → 0x0000 に配置
2回目：トラック1・セクタ5から4セクタ → 0x0E00 に配置
3回目：トラック2・セクタ1から8セクタ → 0x1600 に配置
4回目：トラック3・セクタ1から8セクタ → 0x2600 に配置
5回目：トラック4・セクタ1から8セクタ → 0x3600 に配置
（計35セクタ、約17.5KBをメモリ上の連続領域に再構成）


```
  21:	b0 07                	mov    al,0x7    ; 7セクタ読み込む
  23:	bb 00 00             	mov    bx,0x0    ; メモリ0x0000へ読み込む（ES:BX の BX 部分）
  26:	b9 02 00             	mov    cx,0x2    ; CH=0x00 トラック0, CL=0x02 セクタ2
  29:	e8 35 00             	call   0x61
```

```  
  2c:	81 c3 00 0e          	add    bx,0xe00
  30:	b0 04                	mov    al,0x4
  32:	b9 05 01             	mov    cx,0x105
  35:	e8 29 00             	call   0x61
```

```
  38:	81 c3 00 08          	add    bx,0x800
  3c:	b0 08                	mov    al,0x8
  3e:	b9 01 02             	mov    cx,0x201
  41:	e8 1d 00             	call   0x61
```

```
  44:	81 c3 00 10          	add    bx,0x1000
  48:	b0 08                	mov    al,0x8
  4a:	b9 01 03             	mov    cx,0x301
  4d:	e8 11 00             	call   0x61
```

```
  50:	81 c3 00 10          	add    bx,0x1000
  54:	b0 08                	mov    al,0x8
  56:	b9 01 04             	mov    cx,0x401
  59:	e8 05 00             	call   0x61
```
  
## カーネルへ跳ぶ
```  
  5c:	2e ff 2e 95 7c       	jmp    DWORD PTR cs:0x7c95
```

## 文字列表示関数

```
  84:	53                   	push   bx                    ; BX退避
  85:	ac                   	lods   al,BYTE PTR ds:[si]   ; DS:SI から1バイト読んでALに入れ、SI++。
  86:	0a c0                	or     al,al                 ; ALが0（ヌル終端）か判定
  88:	74 09                	je     0x93                  ; 終端なら抜ける
  8a:	b4 0e                	mov    ah,0xe                ; AH=0x0E（TTY出力モード）
  8c:	bb 00 00             	mov    bx,0x0                ; BH=0（ページ番号）
  8f:	cd 10                	int    0x10                  ; 1文字表示
  91:	eb f2                	jmp    0x85                  ; 次の文字へ
  93:	5b                   	pop    bx
  94:	c3                   	ret  
```

## ディスクからセクタを読み込む共通関数

入力
AL​ : 読み込むセクタ数
BX​ : 書き込み先メモリオフセット（ES:BX）
CX​ : 読み込む位置（CH=トラック, CL=セクタ）
DH​ : ヘッド番号（このコードでは常に0）
DL​ : ドライブ番号（このコードでは常に0）
ES​ : 書き込み先セグメント

トラック番号を画面上に表示 (現代の進捗バー的な意味合い？)
```
  61:	50                   	push   ax
  62:	53                   	push   bx
  63:	8a c5                	mov    al,ch     ; トラック
  65:	04 30                	add    al,0x30   ; 数値をASCII文字に変換
  67:	b4 0e                	mov    ah,0xe    ; BIOS TTY出力
  69:	bb 00 00             	mov    bx,0x0    ; 表示ページ0
  6c:	cd 10                	int    0x10
  6e:	b8 20 0e             	mov    ax,0xe20  ; AH=0x0E, AL=0x20（空白文字）
  71:	cd 10                	int    0x10
```

```
  73:	5b                   	pop    bx
  74:	58                   	pop    ax
  75:	b4 02                	mov    ah,0x2  ; AH=2 → 「セクタ読み込み」機能を指定
  77:	cd 13                	int    0x13    ; BIOSディスク読み込み
  79:	72 01                	jb     0x7c    ; 失敗したらエラー処理へ跳ぶ
  7b:	c3                   	ret    
```

# エラー処理

"Disk Error during Bootstrap... Retrying."を表示してディスク読み込み処理に跳ぶだけ

```
  7c:	be c9 7c             	mov    si,0x7cc9 ; SI="Disk Error during Bootstrap... Retrying.\r\n" 一文字目
  7f:	e8 02 00             	call   0x84      ; 文字列表示関数呼び出し
  82:	eb 91                	jmp    0x15      ; 文字表示が終わったらディスク読み込みへ跳ぶ
```

# 実機でカーネルを動かす為に

今は`int 0x13`のディスク読み込みのCHS設定及びドライブ番号がフロッピー向けとなっているのでブートローダを書き直しました。
QEMUではディスクからの読み込みには成功しますが、カーネルへ跳ぶと画面上に何も表示されません。
実機ではそもそもディスクの読み込みに失敗します。
ブートローダのどこかに不具合があるか、もしくはカーネル内でも`FDD`対象の`int 0x13`があり固まっているのかもしれませんが、カーネルは結構長く書き換えるのが難しいため断念しました。
今後別記事で`int 0x13`をフックし、FDD→HDDへ変換するように改造しようと思います。
実機で失敗する原因は分かりませんが、今回の趣旨は`CP/M86`のブートローダを解析することなので、次回原因を調査します。

## 実機
```bash
nasm -f bin cpm86boot.asm -o cpm86boot.bin
dd if=cpm86boot.bin of=CPM86_10.IMG bs=512 count=1 conv=notrunc
sudo dd if=CPM86_10.IMG of=/dev/sdb bs=512 conv=notrunc
```
<img width="4096" height="3072" alt="40284b43821b1" src="https://github.com/user-attachments/assets/0c546da5-c5b9-41e5-99a9-4f5db5cbca71" />

## QEMU

```
qemu-system-i386 -hda CPM86_10.IMG
```
<img width="772" height="514" alt="截图 2026-07-25 22-59-45" src="https://github.com/user-attachments/assets/a6b7db45-ed37-4944-bb0a-611e7e97962c" />

```cpm86boot.asm
org 0x7C00

start:

    ; 初期化
    cli              ; 割り込み禁止
    cld              ; 方向フラグ=0
    mov    ax,cs     ; DS=CS
    mov    ds,ax
    mov    dx,0x0    ; SS:SP=0x0000:0x7C00
    mov    ss,dx
    mov    sp,0x7c00
    mov    dx,0x80   ; HDD対応 CP/M86には無し
    sti              ; 割り込み許可

    ; CP/M-86 Bootstrap Loader 1.0を表示
    mov    si, msg1
    call   prtstr
    
    mov    ax, 0x51
    mov    es, ax
    
kernelread:
    ; ---- 5回のカーネル読み込み ----
    ; 第1回: 7セクタ, セクタ2, 0x0000へ
    mov    al, 7
    mov    bx, 0x0000
    mov    cx, 0x0002      ; CH=0, CL=2
    call   readdisk

    ; 第2回: 4セクタ, セクタ14, 0x0E00へ
    mov    al, 4
    mov    bx, 0x0E00
    mov    cx, 0x000E      ; CH=0, CL=14
    call   readdisk

    ; 第3回: 8セクタ, セクタ18, 0x1600へ
    mov    al, 8
    mov    bx, 0x1600
    mov    cx, 0x0012      ; CH=0, CL=18
    call   readdisk
    
    ; 第4回: 8セクタ, セクタ26, 0x2600へ
    mov    al, 8
    mov    bx, 0x2600
    mov    cx, 0x001A      ; CH=0, CL=26
    call   readdisk

    ; 第5回: 8セクタ, セクタ34, 0x3600へ
    mov    al, 8
    mov    bx, 0x3600
    mov    cx, 0x0022      ; CH=0, CL=34
    call   readdisk
    
    ; ディスク読み込み成功表示
    mov    si, msg3
    call   prtstr
    
    ; カーネルへ跳ぶ
    jmp 0x0051:0x0000
    
    ; ディスクからセクタを読み込む共通関数
readdisk:
    pusha
    mov    ah, 0x02
    mov    dl, 0x80     ; HDD
    int    0x13
    popa
    jc     err
    ret

prtstr:
    push   bx                    ; BX退避
.loop:
    lodsb                        ; DS:SI から1バイト読んでALに入れ、SI++。
    or     al,al                 ; ALが0（ヌル終端）か判定
    je     .done                  ; 終端なら抜ける
    mov    ah,0xe                ; AH=0x0E（TTY出力モード）
    mov    bx,0x0                ; BH=0（ページ番号）
    int    0x10                  ; 1文字表示
    jmp    .loop                  ; 次の文字へ
.done:
    pop    bx
    ret  

; エラー処理　Disk Error during Bootstrap... Retrying.を表示してディスク読み込み再開
err:
    mov    si,msg2
    call   prtstr
    jmp    kernelread      ; 文字表示が終わったらディスク読み込みへ跳ぶ


hlt_loop:
    hlt
    jmp hlt_loop
    
msg1:
    db "CP/M-86 Bootstrap Loader 1.0", 0
    
msg2:
    db "Disk Error during Bootstrap... Retrying.", 0
    
msg3:
    db "Disk Read OK.", 0
    
times 510-($-$$) db 0
dw 0xAA55
```

# 2026815追記

逆アセンブルして、ディスク読み込み`int 0x13`をしているところを探しましたが、ブートローダ以外でセクタ読み込みをしているところは見つかりませんでした。
```
ndisasm -b 16 CPM86_10.IMG > CPM86_10.asm
grep -in "int 0x13" CPM86_10.asm
```

初めの2つはブートローダ内部
```
test@test-fujitsu:~/kaihatsu$ grep -in "int 0x13" CPM86_10.asm
14:0000001D  CD13              int 0x13
49:00000077  CD13              int 0x13
6152:0000366A  CD13              int 0x13
6546:00003A8F  CD13              int 0x13
6552:00003A9A  CD13              int 0x13
```
